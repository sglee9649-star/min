# 영어 스피킹 평가 플랫폼 (AI Retelling Contest & Speaking Practice)

이 문서는 이 프로젝트의 **단일 기준 명세서**입니다. Claude Code는 모든 작업 시 이 문서를 기준으로 판단합니다.
사용자는 코딩 경험이 거의 없는 교사입니다. 설명은 한국어로, 쉬운 용어로 해주세요.

## 프로젝트 개요

영어 리텔링 대회 운영 + 평소 학생 스피킹 연습(리텔링 / 보고 읽기 / 질문 답변)을 위한 웹앱.

- 피평가자: 한국인 학생 (초1 ~ 고3)
- 규모: 소규모 (참가자 ~30명, 심사위원 수 명)
- 사용 환경: 심사위원·참가자가 각자 태블릿/노트북으로 웹 접속

## 기술 스택 (확정)

| 역할 | 기술 |
|---|---|
| 프론트엔드/서버 | Next.js (App Router, TypeScript) |
| 배포 | Vercel |
| 로그인 / DB / 녹음파일 저장 | Supabase (2단계 이후 도입) |
| 지문·질문·평가 생성 | Claude API |
| 음성 → 텍스트 | OpenAI Whisper API |

## 역할 3분리 (절대 규칙)

1. **참가자 모드** (`/participant`): 대회명, 타이머, 지문, 녹음만 보임. 점수·타인 정보 접근 불가.
2. **심사위원 모드** (`/judge`): 배정된 참가자만, 참가자는 **참가번호로 익명 표시**, 자기 점수만 보임.
3. **관리자 모드** (`/admin`): 전체 관제, 발표 제어, 데이터 삭제/내보내기, 감사 로그.

- 심사위원/관리자 모드는 반드시 인증 뒤에만 진입 가능.
- 점수는 관리자가 "공개" 전환하기 전까지 절대 노출 금지.
- 1단계에서는 임시 접속코드 방식(클라이언트 저장)이며, 2단계에서 Supabase Auth로 교체한다. 임시 방식은 보안이 아니고 자리표시자임을 코드 주석에 명시할 것.

## 핵심 기능 요구사항

### 콘텐츠 생성
- 난이도: 연령별(초1~고3) + 레벨별(Lexile, CEFR/IELTS 대응). 한국인 학습자 기준.
- 카테고리 선택 → 해당 카테고리 안에서 이야기/지문/질문 생성.
- 키워드 직접 입력으로도 생성 가능.
- 과제 유형 3종: 리텔링(듣거나 읽고 다시 말하기), 보고 읽기(낭독), 질문 답변.

### 타이머
- 지문 읽기(준비) 시간, 말하기 시간을 관리자가 각각 설정 가능. 자동 종료.

### 녹음
- 브라우저에서 녹음. **로컬에 먼저 저장 후 업로드** (현장 와이파이 장애 대비).
- 재녹음 정책: 기기 오류 시 재시도 허용 여부 설정 + 재시도 로그 기록.

### 평가
- 평가항목 전체 카탈로그는 `lib/criteria.ts`에 정의 (문서: `docs/EVALUATION_CRITERIA.md`).
- 대회/수업마다 관리자가 카탈로그에서 **항목을 선택하고 비중(가중치)을 설정**해서 평가기준표(루브릭)를 구성한다.
- AI 평가: 전사본 기반, 항목별 점수 + **근거 문장 필수 출력**. AI 점수는 참고용이며 AI:심사위원 반영 비율은 관리자가 설정.
- 심사위원 평가: 인원만큼 추가 가능. 녹음 시작과 동시에 채점 입력 화면 활성화.
- 편차 보정: 최고·최저 제외 또는 표준화 중 선택. 동점 처리 우선순위 항목을 사전 설정.
- 채점 진행률 대시보드 (누가 누구를 아직 안 냈는지).

### 관리자 관제
- 대회명 표시 화면(프로젝션용), 점수 비공개/공개 전환, 엑셀(CSV) 다운로드, 데이터 삭제, 감사 로그(점수 수정 이력).
- 녹음 보관 동의·보관기한(자동 파기) 관리 — 미성년자 음성은 민감정보.

### UI
- 대기/생성 중 화면은 "AI가 생각하는 모습" — 자비스(JARVIS) 느낌의 오브 애니메이션 (`components/JarvisOrb.tsx`).
- 연습 모드(점수 미기록), 결과 발표 화면, 학생별 성장 추이 그래프(수업용).

## 단계별 계획

- [x] **1단계 — 껍데기**: 역할 3분리 화면, 임시 접속코드, 자비스 대기화면, 메뉴 구조
- [ ] **2단계 — 콘텐츠 생성**: Claude API 연동, 난이도/카테고리/키워드 → 지문·질문 생성
- [ ] **3단계 — 녹음 + 타이머**: 읽기/말하기 시간 설정, 자동 종료, 로컬 우선 저장
- [ ] **4단계 — AI 평가**: Whisper 전사 → 루브릭 채점 + 근거
- [ ] **5단계 — 심사위원 모드**: 루브릭 빌더, 심사위원 추가, 익명화, 진행률
- [ ] **6단계 — 관제 + 집계**: 공개 전환, 편차 보정, CSV, 삭제, 발표 화면

각 단계 완료 시 이 체크박스를 갱신할 것.

## 작업 규칙

- 한 번에 한 단계만. 단계를 건너뛰는 대규모 변경 금지.
- API 키는 절대 코드에 넣지 않는다. `.env.local` 사용, `.env.example`에 목록만 유지.
- 실행 확인: `npm run dev` 후 세 모드 화면 진입이 되는지 확인.
